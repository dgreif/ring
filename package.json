{
  "name": "homebridge-ring-alarm",
  "version": "1.2.0",
  "description": "Homebridge plugin for ring secuirty alarm system",
  "main": "lib/index.js",
  "scripts": {
    "test": "tslint **/*.ts",
    "lint": "tslint **/*.ts --fix",
    "build": "tsc --declaration",
    "prepublish": "npm run build && npm test"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/dgreif/homebridge-ring-alarm.git"
  },
  "keywords": [
    "homebridge",
    "homebridge-plugin",
    "ring",
    "alarm"
  ],
  "author": "dgreif",
  "license": "MIT",
  "dependencies": {
    "ring-api": "github:dgreif/ring-api",
    "rxjs": "^6.3.3"
  },
  "devDependencies": {
    "prettier": "^1.14.3",
    "ts-node": "^7.0.1",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.15.0",
    "tslint-plugin-prettier": "^2.0.0",
    "typescript": "^3.1.3"
  },
  "engines": {
    "homebridge": ">=0.4.0"
  }
}
